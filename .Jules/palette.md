## 2026-05-30 - Branding Element Accessibility
**Learning:** Interactive branding elements (e.g., logos that route home) must use semantic tags (`<button>` or `<a>`) with explicit `focus-visible` styling rather than generic `<div>` wrappers to ensure full keyboard navigation and screen reader support.
**Action:** Always use semantic elements and provide focus styles for all clickable regions.
